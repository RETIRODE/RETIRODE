<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:RETIRODE_APP.ViewModels"
             x:Class="RETIRODE_APP.Views.BluetoothDataPage"
             Title="{Binding Title}" >

    <ContentPage.BindingContext>
        <vm:BluetoothViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid BackgroundColor="{StaticResource Secondary}">
            <StackLayout BackgroundColor="{StaticResource Secondary}" VerticalOptions="FillAndExpand" HorizontalOptions="Fill"></StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
    
    